{
  "name": "ts-classify-dev",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "wasm-pack build --target web --no-default-features"
  },
  "devDependencies": {
    "express": "^4.21.0"
  }
}
